version: V_all
name: Versions
objects:
  Versions:
    description: Information on what the homeserver supports.
    fields:
      unstable_features:
        type: "{bool}"
        default: Dict.empty
      versions:
        type: "[string]"
        required: true
